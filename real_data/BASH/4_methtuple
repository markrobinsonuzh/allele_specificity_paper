# We run methtuple on the samples. Methtuple requires the bam files of our BS-seq data to be sorted by query name, so we do that.
# We use sample1 as an example file name.


# Sort bam file by query name
$ samtools sort -n -@ 10 -m 20G -O bam -T _tmp -o QS_sample1_R1_paired.fastq.gz_bismark_bt2_pe.deduplicated_sorted.bam sample1_R1_paired.fastq.gz_bismark_bt2_pe.deduplicated_sorted.bam

# Run methtuple. We looked at the methylation bias plots and saw a bias in positions 1-10 and 96-100 on the R1 reads, and positions 91-100 on the R2 reads.
# These positions are skipped when running methtuple. We use the --sc option for strand collapse.
$ methtuple --sc --gzip -m 2 --ir1p 1-10,96-100 --ir2p 91-100 QS_sample1_R1_paired.fastq.gz_bismark_bt2_pe.deduplicated_sorted.bam 
